name: CI

# Fast feedback: does it still compile, and does the smoke-test binary exist.
on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Cache whisper.cpp build
        uses: actions/cache@v4
        with:
          path: vendor/whisper.cpp
          key: whisper-${{ runner.os }}-${{ hashFiles('scripts/bootstrap_whisper.sh') }}

      - name: Build whisper.cpp
        run: ./scripts/bootstrap_whisper.sh

      - name: Build app bundle (no DMG)
        run: ./scripts/build_app.sh --no-dmg

      - name: Check the bundle is signed and versioned
        run: |
          codesign --verify --deep dist/AlejoVoice.app
          /usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" \
            dist/AlejoVoice.app/Contents/Info.plist
